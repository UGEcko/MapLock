import { notesBetween } from "https://deno.land/x/remapper@2.1.0/src/mod.ts"

/**
* uses an end time, NOT A DURATION
* @param timeStart
* @param timeEnd
* @param leftData
* @param rightData
*/

export function LRnotes(timeStart, TimeEnd, leftData: any, rightData: any) {
  //i have no clue if this works
  if(note.type === 0, (leftNote => {
    leftNote.customData = leftData
  });
  if(note.type === 1, (rightNote => {
    rightNote.customData = rightData
  })
